# 发布验证

2026-09-25 在整理后的公开源码副本运行：

```text
python -m unittest discover -s gui_delegate/tests -p "test_*.py"
Ran 115 tests in 2.206s
OK
```

测试复用了开发环境已安装的 Python 依赖，工作目录与导入源码为本次公开副本。覆盖契约、动作范围、低置信与恢复、输入保护、路由、浏览器会话、语义规则和 Windows 观察等受控逻辑。

另外，13 项浏览器适配器边界检查、21 项标签清理检查、5 项结果呈现断言通过。真实 Python worker 与控制器的通信／恢复／取消测试也通过；浏览器是受控替身、Jev 调用为 0。后者需本地运行环境和临时测试启用状态，公开包不携带该机器状态。

这些测试使用合成数据及替身；本次未重复进行付费 Jev 请求、真实网页完整任务或新机器全新安装。不把离线通过等同于所有网站兼容、生产稳定或独立完成任意 GUI 任务。

本次发布还检查了个人路径、常见凭据格式与上传文件范围。只发布源码、使用说明、合成样例和汇总指标；本机浏览器配置、运行日志、密钥、个人任务数据、依赖目录和回滚档案不在上传列表中。
